spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: localhost
    port: 2525
    username: test
    password: test

app:
  mail:
    from: test@example.com
  frontend:
    url: http://localhost:8080

jwt:
  secret: test-secret-key-for-integration-tests-only-not-used-in-production
  expiration: 86400000

cloudinary:
  cloud-name: test
  api-key: test
  api-secret: test